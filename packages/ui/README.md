# packages/ui

Shared, presentation-only component library consumed by `apps/frontend`. No business logic, no data fetching — components receive data and callbacks as props.

Populated starting **Phase 6 — Founder Dashboard**.
