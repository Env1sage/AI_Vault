# tests/integration

Tests that exercise real boundaries between modules or services (e.g. backend ↔ database, backend ↔ worker queue) using local/dockerized dependencies rather than mocks. See the Testing Strategy in [`Docs/00_ENGINEERING_HANDBOOK.md`](../../Docs/00_ENGINEERING_HANDBOOK.md).
