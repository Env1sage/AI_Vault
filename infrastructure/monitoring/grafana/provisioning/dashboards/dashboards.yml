apiVersion: 1

providers:
  - name: vault-dashboards
    orgId: 1
    folder: "AI Project Vault"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
