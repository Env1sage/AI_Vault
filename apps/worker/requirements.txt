-e ../../packages/shared

celery[redis]>=5.4,<6

# Content Extraction Framework (Phase 5) — deterministic text extraction
# only, no AI/ML. Worker-only: the backend never extracts file content.
pypdf>=5.0,<6
python-docx>=1.1,<2
openpyxl>=3.1,<4
python-pptx>=1.0,<2
