VITE_API_BASE_URL=http://localhost:8000

# Google Identity Services client ID — same value as the backend's
# GOOGLE_CLIENT_ID (repo-root .env). Not a secret; safe to expose to the browser.
VITE_GOOGLE_CLIENT_ID=
